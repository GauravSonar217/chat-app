import React from "react";

const Header = () => (
  <header className="header" style={{ background: '#fff', padding: '1rem', borderBottom: '1px solid #eee' }}>
    <h1>Admin Panel</h1>
    {/* Add user info, logout, etc. */}
  </header>
);

export default Header;
